# Conventions — how code and notes here are structured.

- Language / style: (e.g. Python, type hints, small functions).
- Naming: (modules, tests, configs).
- Notes: no line cap; use focused headings, `path#symbol` refs, `[[cross-link]]`, one fact per place. Context is index-first and full note bodies are read on demand.

See [[gotchas]] for what breaks if you ignore these.
